# 说明：本文件刻意不设置 packageManager，也不开启 manage-package-manager-versions。
# 原因：一旦在 package.json 中钉死 pnpm 版本，pnpm 会自动去下载对应版本的 @pnpm/exe，
# 而该构建在部分版本（如 11.12.0）缺失二进制，会导致服务器上报 ERR_PNPM_BROKEN_PNPM_RELEASE。
# 直接使用各环境自行安装的 pnpm 即可。

# 依赖安装时允许执行构建脚本的名单见 pnpm-workspace.yaml
