# 服务端渲染（SSR）运行参数，由 dist/server/entry.mjs 读取。
# 复制为 .env 后按需修改；该文件不要提交到仓库。

# Node 服务监听端口（默认 8080）
PORT=4321

# Node 服务监听地址。
# 由宝塔 Nginx 反向代理时用 127.0.0.1 更安全，不要直接暴露到公网。
HOST=127.0.0.1

# 生产环境标记
NODE_ENV=production
